<?php
        $arreglo = [1,2,3,4,5];
        $cantidadDatos = count($arreglo);
        $iterar;
        for($iterar = 0; $iterar < $cantidadDatos; $iterar++){
            var_dump($arreglo[$iterar]);
        }

?>